import Component from "../../components/Component";

class Directive extends Component {

    constructor(element) {
        super(element);

        this.handleEvents();
    }

    handleEvents() {
    }
}

export default Directive;
